# 🕵️ NotNahid Intelligence Tool

[![Open in GitHub Codespaces](https://github.com/codespaces/badge.svg)](https://codespaces.new/YOUR_GITHUB_USERNAME/YOUR_REPO_NAME)

### How to use:
1. Click the "Open in GitHub Codespaces" button above.
2. Wait for the terminal to load.
3. Type this command:
   ```bash
   pip install -r requirements.txt && sudo apt update && sudo apt install chromium-driver -y && streamlit run dashboard_pro.py
